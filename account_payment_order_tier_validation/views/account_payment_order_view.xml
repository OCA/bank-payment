<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <record id="view_account_payment_order_filter" model="ir.ui.view">
        <field name="name">account_payment_order_tier_validation</field>
        <field name="model">account.payment.order</field>
        <field
            name="inherit_id"
            ref="account_payment_order.account_payment_order_search"
        />
        <field name="arch" type="xml">
            <search position="inside">
                <group expand="0" string="Need actions">
                    <filter
                        name="needs_review"
                        string="Needs my Review"
                        domain="[('reviewer_ids','in',uid), ('state', 'not in', ['done', 'cancel'])]"
                        help="To review"
                    />
                    <filter
                        name="tier_validated"
                        string="Validated"
                        domain="[('validated', '=', True)]"
                        help="Account Paymen Orders validated and ready to be confirmed"
                    />
                </group>
            </search>
        </field>
    </record>
</odoo>
